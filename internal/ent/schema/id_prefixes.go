package schema

const (
	// ApplicationPrefix is the prefix for all application IDs owned by the Virtual Machine API
	ApplicationPrefix = "virt"

	// VirtualMachinePrefix is the prefix for all Virtual Machine IDs
	VirtualMachinePrefix = ApplicationPrefix + "mac"
)
